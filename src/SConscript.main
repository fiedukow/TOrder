# vim: ft=python

env = Environment(
                    CCFLAGS = '-O2 -std=c++0x',
                    LINKFLAGS = '-std=c++0x'
                 )
sourceTargets = Glob('*.cpp')

env.Program('TOrder',sourceTargets)
